set(project_files
        ${CMAKE_CURRENT_SOURCE_DIR}/../../../inc/iothubtransporthttp.h
        ${CMAKE_CURRENT_SOURCE_DIR}/../../../src/iothubtransporthttp.c
		)
	
set(common_files 
	)
	
# Add the paterns of the filenames to be deleted before pushing local update to remote repo.
set(exclude_files
	)
